<div class="row wrapper border-bottom white-bg page-heading">
  <div class="col-lg-12">
    <h2>Productos</h2>
    <ol class="breadcrumb">
      <li>
        <a href="<%= root_path %>">Inicio</a>
      </li>
      <li>
        <a href="<%= gestion_productos_path %>">Productos</a>
      </li>
      <li class="active">
        <strong>Editando producto <%= @producto.nombre %></strong>
      </li>
    </ol>
  </div>
</div>

<div class="row wrapper wrapper-content animated fadeInRight">
  <%= render partial: 'form', locals: {producto: @producto} %>
</div>